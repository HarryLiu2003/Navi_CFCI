# .gcloudignore for database-service

# Node.js dependencies - these should be installed during the Docker build
node_modules/

# Build artifacts (if any, although Dockerfile handles this)
dist/
*.tsbuildinfo

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# OS generated files
.DS_Store
Thumbs.db

# Environment files - sensitive info should be in Secret Manager
.env

# Test results / coverage
coverage/
.nyc_output/

# Git directory (usually ignored by default, but good practice)
.git/