# Navi CFCI Root Environment Configuration
#
# NOTE: This project now uses service-specific .env files.
# Please see the individual .env.example files in each service directory:
# - services/api_gateway/.env.example
# - services/interview_analysis/.env.example
# - services/sprint1_deprecated/.env.example
# - frontend/.env.example
#
# This root .env file is now only used for Docker Compose environment variables.

# Service Ports (used by docker compose)
API_GATEWAY_PORT=8000
INTERVIEW_ANALYSIS_PORT=8001
SPRINT1_DEPRECATED_PORT=8002
FRONTEND_PORT=3000

# Development Settings (used globally)
DEBUG=true
NODE_ENV=development
